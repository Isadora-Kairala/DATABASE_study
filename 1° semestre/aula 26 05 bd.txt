--criar novo bd
CREATE DATABASE vendas2026

USE vendas2026	

--criar a tabela Marca ja com a chave primaria:
CREATE TABLE marca(
	idMarca int PRIMARY KEY, --esse comando torna o campo como PK
	nome varchar(80),
	situacao char(1)
)


--cadastrar marcas:
INSERT INTO marca
VALUES (1, 'COCA-COLA', 'A')

INSERT INTO marca
VALUES (2, 'PEPSI', 'A')

INSERT INTO marca
VALUES (3, 'ANTARCTICA', 'A')

SELECT * FROM marca

--testando propriedade de valor unico da pk
INSERT INTO marca (nome,situacao)
VALUES('Teste', 'A')


DROP TABLE produto






--criar  a tabela Produto com PK com numeracao automatica e com fk para marca

CREATE TABLE produto(
	idPro int PRIMARY KEY IDENTITY(1,1),
	nome varchar(100),
	preco money,
	cor varchar(40),
	idMarca int FOREIGN KEY REFERENCES marca(idMarca)
)

--CADASTRAR  PRODUTOS PARA TESTAR AUTONUMERAÇÃO DA PK:
INSERT INTO produto(nome, preco)
VALUES ('COCA LATA 340 ml', 8.94)

--TESTAR INTEGRIDADE REFERENCIAL DA FK COM UPDATE DO REGISTRO
UPDATE produto SET idMarca = 20
WHERE idPro = 1

UPDATE produto SET idMarca = 3
WHERE idPro = 1

SELECT * FROM produto
--criar nova tabela para cadastro de fornecedores:
CREATE TABLE forncedor(
	idFor int PRIMARY KEY IDENTITY(1,1),
	razaoSocial varchar(50),
	cnpj varchar(20)
	)


	--alterar a estrutura da tabela produto para uma nova FK ligando ao fornecedor:
	ALTER TABLE produto
		ADD idFor INT FOREIGN KEY REFERENCES fornecedor(idfor)

	--exercicios de fixaçao
	1. CADASTRE 3 PRODUTOS INFORMANDO SÓ O NOME E O PRECO
	2. CADASTRE 4 FORNECEDORES PREENCHENDO TODOS OS CAMPOS
	3. ALTERE O NOME DA MARCA 'SKOL' PARA 'LEVÍSSIMA'
	4. CADASTRE 2 PRODUTOS INFORMANDO TODOS OS CAMPOS DA TABELA
	5. ATUALIZE O CNPJ DO FORNECEDOR CUJO idFor É 3 PARA TER VALOR '123456789'
	6. ATUALIZE TODOS OS PRODUTOS QUE NÃO TEM MARCA PARA TEREM ALGUM idMarca
	7. EXCLUA O PRODUTO DE CÓDIGO = 2
	8. EXCLUA A MARCA DE CÓDIGO = 4
	9. ACRESCENTE 10% A MAIS NO PRECO DOS PRODUTOS QUE TEM PRECO MAIOR QUE ZERO 
	10. EXCLUA (APAGUE) A TABELA FORNECEDOR 

